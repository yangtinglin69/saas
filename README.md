# Affiliate SaaS - 多租戶聯盟行銷平台

## 🎯 功能特色

- **多租戶架構**：一個系統支援多個站點
- **主網域管理**：預設多個主網域，用戶可創建子網域
- **用戶認證**：Supabase Auth 登入/註冊
- **站點管理**：每個用戶可管理多個站點
- **資料隔離**：Row Level Security 確保資料安全

## 📁 架構說明

```
管理後台 (admin.yourdomain.com)
├── /login          - 登入
├── /register       - 註冊
├── /dashboard      - 站點列表
└── /dashboard/sites/[siteId]  - 管理特定站點

前台站點 (*.brand-a.com, *.brand-b.com...)
└── 根據網域自動顯示對應站點內容
```

## 🚀 部署步驟

### 1. Supabase 設定

1. 建立新的 Supabase 專案
2. 執行 `supabase/schema.sql` 建立資料表
3. 執行 `supabase/seed.sql` 建立預設主網域
4. 複製 Project URL 和 anon key

### 2. 修改主網域

編輯 `supabase/seed.sql`，改成你的網域：

```sql
INSERT INTO domains (id, domain, name) VALUES
  ('brand-a', 'your-domain-a.com', '品牌 A'),
  ('brand-b', 'your-domain-b.com', '品牌 B');
```

### 3. DNS 設定

每個主網域需要設定 Wildcard CNAME：

| 類型 | 名稱 | 值 |
|------|------|-----|
| CNAME | * | cname.vercel-dns.com |
| CNAME | @ | cname.vercel-dns.com |

### 4. Vercel 部署

1. 上傳到 GitHub
2. Vercel 連接 repo
3. 設定環境變數：
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
4. 部署

### 5. Vercel 網域設定

在 Vercel Project Settings → Domains 加入：
- 管理後台域名
- 所有主網域（含 wildcard）

## 🔒 資安說明

1. **Row Level Security**：所有資料表都啟用 RLS
2. **用戶隔離**：每個用戶只能存取自己的站點
3. **Supabase Auth**：使用標準的 JWT 認證

## 📝 開發中功能

- [ ] 產品管理頁面
- [ ] 模組管理頁面
- [ ] 網站設定頁面
- [ ] 匯入中心
- [ ] AI 內容生成
